# Asad Iftikhar — Resume Website

React single-page app with Home, Experience, Skills, Work, and Contact.

```bash
cd "C:\Users\asad.iftikhar\Music\asad-portfolio"
npm install
npm run dev
```

Then open the local URL Vite prints (usually http://localhost:5173).

```bash
npm run build
npm run preview
```
